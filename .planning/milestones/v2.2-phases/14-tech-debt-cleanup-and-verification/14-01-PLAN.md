---
phase: 14-tech-debt-cleanup-and-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09-mobile-header-and-pricing-cleanup/09-VERIFICATION.md
  - src/components/Connect.astro
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - PRIC-07
  - MOBL-01

must_haves:
  truths:
    - "Phase 9 has a VERIFICATION.md confirming PRIC-07 (no duplicate pricing cards in Accommodations) and MOBL-01 (mobile header shows brand text) are satisfied with grep and Playwright evidence"
    - "The orphaned src/components/Connect.astro file is deleted — only Contact.astro exists for the contact section"
    - "REQUIREMENTS.md traceability table shows PRIC-07 and MOBL-01 as Complete with correct phase assignments"
    - "REQUIREMENTS.md checkbox entries for PRIC-07 and MOBL-01 are marked [x] (checked)"
  artifacts:
    - path: ".planning/phases/09-mobile-header-and-pricing-cleanup/09-VERIFICATION.md"
      provides: "Formal verification document for Phase 9 requirements"
      contains: "status: passed"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability showing PRIC-07 and MOBL-01 as Complete"
      contains: "Complete"
  key_links:
    - from: ".planning/phases/09-mobile-header-and-pricing-cleanup/09-VERIFICATION.md"
      to: "tests/viewport.spec.ts"
      via: "Playwright test reference for MOBL-01 automated coverage"
      pattern: "header brand text visible"
    - from: ".planning/phases/09-mobile-header-and-pricing-cleanup/09-VERIFICATION.md"
      to: "src/components/Nav.astro"
      via: "Source evidence for brand span class without hidden sm:block"
      pattern: "text-xl sm:text-2xl"
    - from: ".planning/phases/09-mobile-header-and-pricing-cleanup/09-VERIFICATION.md"
      to: "src/components/Accommodations.astro"
      via: "Source evidence for Rate Card removal and pricing teaser"
      pattern: "pricing-calculator"
---

<objective>
Close Phase 9 verification gaps, delete the orphaned Connect.astro file, and update REQUIREMENTS.md traceability.

Purpose: The v2.2 milestone audit identified that Phase 9 executed PRIC-07 and MOBL-01 correctly (commit 7b78dfa) but lacks a formal VERIFICATION.md documenting the evidence. Additionally, the orphaned Connect.astro file (superseded by Contact.astro in Phase 4) creates misleading grep results, and REQUIREMENTS.md still shows PRIC-07 and MOBL-01 as Pending.

Output: Phase 9 VERIFICATION.md with grep and Playwright evidence; Connect.astro deleted; REQUIREMENTS.md updated with both checkbox and traceability table entries marked Complete.
</objective>

<execution_context>
@/home/evan/.claude/get-shit-done/workflows/execute-plan.md
@/home/evan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-tech-debt-cleanup-and-verification/14-RESEARCH.md
@.planning/phases/09-mobile-header-and-pricing-cleanup/09-01-SUMMARY.md

<interfaces>
<!-- Evidence sources the executor needs to cite in VERIFICATION.md -->

From src/components/Nav.astro (line 31, brand span):
```html
<span class="font-serif text-xl sm:text-2xl text-stone-800 group-hover:text-brand transition-colors">Timber &amp; Threads</span>
```
Note: `hidden sm:block` is absent from brand span. Line 32 underline div retains `hidden sm:block`.

From src/components/Accommodations.astro (line 125, pricing teaser):
```html
<p class="text-center text-stone-600 mt-8">
  Stays from <strong class="text-stone-800">$60/night per person</strong> — <a href="#pricing-calculator" class="text-brand hover:underline">see full pricing and estimate your stay</a>.
</p>
```
Note: Rate Card block (formerly lines 124-150) is fully absent.

From tests/viewport.spec.ts (lines 7-10):
```typescript
test('header brand text visible', async ({ page }) => {
  // Brand text is always visible at all viewports — Phase 9 removed hidden sm:block
  await expect(page.locator('header').getByText('Timber & Threads')).toBeVisible();
});
```
This test runs across all three viewport projects (desktop-1280, mobile-375, mobile-320).

From src/components/Connect.astro: Orphaned file with `<section id="contact">` containing only a Facebook social card. Not imported by any file. Superseded by Contact.astro (Phase 4) which has full contact form, phone, email, address, and Facebook card.

From .planning/REQUIREMENTS.md — current state of entries to update:
- Line 12: `- [ ] **PRIC-07**: Duplicate pricing tier cards removed from Accommodations section (kept in Pricing section only)`
- Line 19: `- [ ] **MOBL-01**: Mobile header displays "Timber & Threads" text title alongside logo`
- Line 181: `| PRIC-07 | Phase 9 → Phase 14 (verify) | Pending |`
- Line 185: `| MOBL-01 | Phase 9 → Phase 14 (verify) | Pending |`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Phase 9 VERIFICATION.md with grep and Playwright evidence</name>
  <files>.planning/phases/09-mobile-header-and-pricing-cleanup/09-VERIFICATION.md</files>
  <action>
Create `.planning/phases/09-mobile-header-and-pricing-cleanup/09-VERIFICATION.md` with the following structure and content.

**IMPORTANT:** The file goes in the Phase 9 directory, NOT the Phase 14 directory. The success criterion says "Phase 9 has a VERIFICATION.md."

Before writing, run these grep commands to collect live evidence:

```bash
# MOBL-01: Confirm hidden sm:block only on underline div, not brand span
grep -n "hidden sm:block" src/components/Nav.astro

# PRIC-07: Confirm Rate Card is gone
grep -c "Rate Card" src/components/Accommodations.astro

# PRIC-07: Confirm pricing teaser link is present
grep -c "pricing-calculator" src/components/Accommodations.astro
```

Then write the VERIFICATION.md with this content:

```markdown
---
phase: 09-mobile-header-and-pricing-cleanup
status: passed
requirements: [MOBL-01, PRIC-07]
verified: 2026-03-03
---

# Phase 9 Verification: Mobile Header and Pricing Cleanup

## MOBL-01: Mobile Header Brand Text

**Result: PASSED**

**Evidence:**
- src/components/Nav.astro line 31: brand span class is `font-serif text-xl sm:text-2xl text-stone-800 group-hover:text-brand transition-colors`
- `hidden sm:block` is absent from the brand span — text is always visible at all viewports
- Underline decoration div (line 32) retains `hidden sm:block` as intended (decorative hover effect, desktop-only)
- Playwright test `header brand text visible` in tests/viewport.spec.ts:7 asserts visibility across all three viewport projects (desktop-1280, mobile-375, mobile-320)

**Grep verification:**
- `grep -n "hidden sm:block" src/components/Nav.astro` returns line 32 only (the underline div) — brand span is not in this output

## PRIC-07: Duplicate Pricing Cards Removed from Accommodations

**Result: PASSED**

**Evidence:**
- src/components/Accommodations.astro: Rate Card block (formerly lines 124-150 with `<!-- Rate Card -->` comment, `mt-12` div, two tier cards, minimums text, "Estimate Your Stay" CTA) is fully absent
- Replaced with single pricing teaser paragraph linking to `#pricing-calculator`
- Authoritative pricing content remains exclusively in src/components/PricingSection.astro

**Grep verification:**
- `grep -c "Rate Card" src/components/Accommodations.astro` returns 0
- `grep -c "pricing-calculator" src/components/Accommodations.astro` returns 1

**Commit:** 7b78dfa (feat: show brand text on mobile, remove duplicate pricing cards)
```

Use the actual grep output from the commands you ran to confirm the evidence matches. If any grep result differs from what is documented here, update the VERIFICATION.md content accordingly.
  </action>
  <verify>
    <automated>cd /home/evan/Projects/clients/timberandthreads-v2 && test -f .planning/phases/09-mobile-header-and-pricing-cleanup/09-VERIFICATION.md && grep -c "status: passed" .planning/phases/09-mobile-header-and-pricing-cleanup/09-VERIFICATION.md && echo "VERIFICATION.md exists and has passed status"</automated>
  </verify>
  <done>
- 09-VERIFICATION.md exists at `.planning/phases/09-mobile-header-and-pricing-cleanup/09-VERIFICATION.md`
- Contains frontmatter with `status: passed` and `requirements: [MOBL-01, PRIC-07]`
- MOBL-01 section cites Nav.astro brand span class, absence of `hidden sm:block`, and Playwright test reference
- PRIC-07 section cites absent Rate Card block, present pricing teaser, and grep verification
- All grep evidence matches live codebase state
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete orphaned Connect.astro and update REQUIREMENTS.md traceability</name>
  <files>src/components/Connect.astro, .planning/REQUIREMENTS.md</files>
  <action>
**Step 1: Verify Connect.astro has zero importers, then delete it.**

Run this grep to confirm no file imports Connect.astro:

```bash
grep -r "Connect" src/ --include="*.astro" --include="*.ts" --include="*.tsx" | grep -i "import"
```

Expected: no output referencing Connect.astro as an import. (Contact.astro imports will appear — those are fine.)

After confirming zero importers, delete the file:

```bash
rm src/components/Connect.astro
```

Verify deletion:

```bash
test ! -f src/components/Connect.astro && echo "Connect.astro deleted"
```

**Step 2: Update REQUIREMENTS.md in TWO locations.**

Open `.planning/REQUIREMENTS.md` and make these four edits:

**Location 1 — Requirement definition checkboxes (near top of v2.2 section):**

Change:
```
- [ ] **PRIC-07**: Duplicate pricing tier cards removed from Accommodations section (kept in Pricing section only)
```
To:
```
- [x] **PRIC-07**: Duplicate pricing tier cards removed from Accommodations section (kept in Pricing section only)
```

Change:
```
- [ ] **MOBL-01**: Mobile header displays "Timber & Threads" text title alongside logo
```
To:
```
- [x] **MOBL-01**: Mobile header displays "Timber & Threads" text title alongside logo
```

**Location 2 — Traceability table (bottom of file):**

Change:
```
| PRIC-07 | Phase 9 → Phase 14 (verify) | Pending |
```
To:
```
| PRIC-07 | Phase 9 | Complete |
```

Change:
```
| MOBL-01 | Phase 9 → Phase 14 (verify) | Pending |
```
To:
```
| MOBL-01 | Phase 9 | Complete |
```

Also update MAPS-01 and MAPS-02 if they are still marked Pending — Phase 12 completed these. Check their current status and update to Complete if appropriate (the maps route embed was implemented in commit history and the no-API-key pb= format was used per decision log, so MAPS-02 about API key restriction is N/A — update the description if needed to reflect the no-API-key approach).

**CRITICAL:** Both locations (checkboxes AND traceability table) must be updated. Updating only one creates inconsistency.
  </action>
  <verify>
    <automated>cd /home/evan/Projects/clients/timberandthreads-v2 && test ! -f src/components/Connect.astro && grep -c "\[x\] \*\*PRIC-07\*\*" .planning/REQUIREMENTS.md && grep -c "\[x\] \*\*MOBL-01\*\*" .planning/REQUIREMENTS.md && grep "PRIC-07" .planning/REQUIREMENTS.md | grep -c "Complete" && grep "MOBL-01" .planning/REQUIREMENTS.md | grep -c "Complete" && echo "All updates verified"</automated>
  </verify>
  <done>
- src/components/Connect.astro is deleted (confirmed via `test ! -f`)
- REQUIREMENTS.md has `[x]` checkbox for PRIC-07
- REQUIREMENTS.md has `[x]` checkbox for MOBL-01
- REQUIREMENTS.md traceability table shows `PRIC-07 | Phase 9 | Complete`
- REQUIREMENTS.md traceability table shows `MOBL-01 | Phase 9 | Complete`
- No orphaned Connect.astro file exists in the components directory
  </done>
</task>

</tasks>

<verification>
- `.planning/phases/09-mobile-header-and-pricing-cleanup/09-VERIFICATION.md` exists with `status: passed` in frontmatter
- `grep -c "status: passed" .planning/phases/09-mobile-header-and-pricing-cleanup/09-VERIFICATION.md` returns 1
- `test ! -f src/components/Connect.astro` succeeds (file deleted)
- `grep -c "\[x\] \*\*PRIC-07\*\*" .planning/REQUIREMENTS.md` returns 1
- `grep -c "\[x\] \*\*MOBL-01\*\*" .planning/REQUIREMENTS.md` returns 1
- `grep "PRIC-07" .planning/REQUIREMENTS.md` shows "Complete" (not "Pending")
- `grep "MOBL-01" .planning/REQUIREMENTS.md` shows "Complete" (not "Pending")
</verification>

<success_criteria>
1. Phase 9 has a VERIFICATION.md confirming PRIC-07 and MOBL-01 are satisfied, with grep evidence and Playwright test references
2. The orphaned Connect.astro file is deleted — only Contact.astro exists for the contact section
3. REQUIREMENTS.md shows PRIC-07 and MOBL-01 as Complete in both the checkbox entries and the traceability table
</success_criteria>

<output>
After completion, create `.planning/phases/14-tech-debt-cleanup-and-verification/14-01-SUMMARY.md`
</output>
