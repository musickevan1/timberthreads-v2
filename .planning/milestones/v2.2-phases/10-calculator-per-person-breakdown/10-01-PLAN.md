---
phase: 10-calculator-per-person-breakdown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/PricingCalculator.tsx]
autonomous: false
requirements: [PRIC-08]

must_haves:
  truths:
    - "After entering a valid group size and number of nights, a per-person cost line appears below the Estimated Total in the calculator breakdown panel"
    - "The per-person cost updates in real time as the visitor adjusts group size, nights, or the food option toggle"
    - "The per-person line is not shown when calculator inputs are below minimum constraints (fewer than 4 persons or fewer than 2 nights)"
    - "The per-person line is visually subordinate to the bold brand-colored Estimated Total"
  artifacts:
    - path: "src/components/PricingCalculator.tsx"
      provides: "Per-person cost derived value and display line"
      contains: "perPerson"
  key_links:
    - from: "perPerson constant"
      to: "formatCurrency(perPerson)"
      via: "derived value rendered in JSX"
      pattern: "formatCurrency\\(perPerson\\)"
---

<objective>
Add a per-person cost breakdown line to the pricing calculator so group organizers can see and communicate the individual cost to each participant.

Purpose: Group organizers — the primary calculator users — need the per-person number to share with their group. Right now they must manually divide the total. This adds that line directly below the Estimated Total inside the same divider block.

Output: Updated `PricingCalculator.tsx` with a per-person cost line that updates in real time.
</objective>

<execution_context>
@/home/evan/.claude/get-shit-done/workflows/execute-plan.md
@/home/evan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-calculator-per-person-breakdown/10-CONTEXT.md
@.planning/phases/10-calculator-per-person-breakdown/10-RESEARCH.md
@src/components/PricingCalculator.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From src/components/PricingCalculator.tsx (lines 4-6):
```typescript
function formatCurrency(amount: number): string {
  return `$${amount.toLocaleString()}`;
}
```

Existing state (line 60-63):
```typescript
const [groupSize, setGroupSize] = useState(6);
const [nights, setNights] = useState(3);
const [includeMeals, setIncludeMeals] = useState(false);
```

Existing calculation block ends at line 81 with `total = accommodationCost + foodCost;`

Existing divider block (lines 168-174) — insertion target:
```tsx
{/* Divider */}
<div class="border-t border-stone-200 pt-3 mt-3">
  <div class="flex justify-between items-center">
    <span class="font-serif text-lg text-stone-800">Estimated Total</span>
    <span class="text-2xl font-bold text-brand">{formatCurrency(total)}</span>
  </div>
</div>
```

Slider constraints: groupSize min={4} max={12}, nights min={2} max={7}. These enforce minimums at the UI level, so groupSize >= 4 and nights >= 2 are always true during normal interaction.

The breakdown panel has `aria-live="polite"` on its container — do NOT add another aria-live to the per-person line.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add per-person cost calculation and display line to PricingCalculator.tsx</name>
  <files>src/components/PricingCalculator.tsx</files>
  <action>
Make two edits to `src/components/PricingCalculator.tsx`:

**Edit 1 — Add derived constant (after line 81, after the closing `}` of the else block):**

```typescript
const perPerson = Math.round(total / groupSize);
```

This is a derived value — do NOT use `useState`. It recalculates on every render automatically when `total` or `groupSize` change. `Math.round` keeps it as a whole dollar amount, consistent with `formatCurrency()` which formats integers (no cents).

**Edit 2 — Add per-person display line inside the divider block (after the "Estimated Total" flex row, before the closing `</div>` of the divider block):**

```tsx
<div class="flex justify-between items-center mt-1">
  <span class="text-sm text-stone-500">Per person ({groupSize} guests)</span>
  <span class="text-sm font-medium text-stone-600">{formatCurrency(perPerson)}</span>
</div>
```

**Styling rationale (locked decisions from CONTEXT.md):**
- `text-sm` vs the total's `text-2xl` — visually secondary per user decision
- `text-stone-500` / `text-stone-600` vs the total's `text-brand` — lighter, subordinate
- `mt-1` keeps it close to the total within the same divider block
- `font-medium` on the amount gives it just enough weight to be clearly readable without competing with `font-bold` on the total
- Left-label / right-amount layout matches the existing breakdown line items (accommodation, meals, total)

**What NOT to do:**
- Do NOT add `useState` for `perPerson` — it is derived, not independent state
- Do NOT add `aria-live` to the per-person line — the parent panel already has `aria-live="polite"`
- Do NOT use `toFixed(2)` or show cents — `formatCurrency()` with whole dollars is the project convention
- Do NOT add a conditional guard like `meetsMinimums` — the sliders enforce min={4} and min={2} so values below minimums are unreachable through normal interaction, and the initial state (6, 3) is above both minimums
- Do NOT place the per-person line outside the divider block — CONTEXT.md locks it "directly below the Estimated Total inside the same divider block"
- Do NOT add helper text like "Share with your group" — the label is self-explanatory and extra text adds clutter
  </action>
  <verify>
    <automated>cd /home/evan/Projects/clients/timberandthreads-v2 && npx astro check 2>&1 | tail -5 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
- `PricingCalculator.tsx` contains a `perPerson` derived constant computed as `Math.round(total / groupSize)`
- A per-person display line appears inside the divider block, directly below the "Estimated Total" row
- The per-person line uses `text-sm text-stone-500` (label) and `text-sm font-medium text-stone-600` (amount) — visually subordinate to the bold brand-colored total
- The label reads "Per person (N guests)" where N is the current `groupSize` value
- The amount uses `formatCurrency(perPerson)` — whole dollar, no cents
- `astro check` passes with no type errors
- `npm run build` succeeds with zero errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify per-person breakdown in browser</name>
  <what-built>Per-person cost line added to the pricing calculator breakdown panel, positioned below the Estimated Total, updating in real time as inputs change.</what-built>
  <how-to-verify>
1. Run `npm run dev` and open http://localhost:4321
2. Scroll to the Pricing Calculator section
3. With default values (6 guests, 3 nights, meals off):
   - Confirm "Estimated Total" shows $1,080
   - Confirm a per-person line below it shows "Per person (6 guests)" with $180
   - Confirm the per-person line is visually smaller and lighter than the total
4. Adjust group size slider to 10:
   - Total should update to $1,800
   - Per-person should update to "Per person (10 guests)" with $180
5. Adjust group size slider to 11 (triggers flat rate):
   - Amber callout banner should appear
   - Total should update to $1,800
   - Per-person should update to "Per person (11 guests)" with $164
6. Toggle "Include Meals" on (with 11 guests, 3 nights):
   - Total should increase (accommodation $1,800 + meals $412.50 ≈ $2,213)
   - Per-person should show rounded amount (~$201)
7. Set group size back to 4 (minimum), nights to 2 (minimum):
   - Per-person line should still display (minimums are met)
8. Check mobile viewport (375px): per-person line should not overflow
  </how-to-verify>
  <resume-signal>Type "approved" if the per-person line displays correctly and updates in real time, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
- `npx astro check` passes with zero type errors
- `npm run build` completes with zero errors
- The per-person line is visible in the browser below the Estimated Total
- Adjusting any input (group size slider, nights slider, meals toggle) updates the per-person amount in real time
- The per-person line is visually subordinate to the total (smaller text, lighter color)
</verification>

<success_criteria>
1. After entering a valid group size and number of nights, the calculator breakdown panel displays a per-person cost line below the total estimate
2. The per-person cost updates in real time as the visitor adjusts group size, nights, or the food option
3. The per-person breakdown is not shown when the calculator inputs are below minimum constraints (fewer than 4 persons or fewer than 2 nights) — enforced by slider min values
</success_criteria>

<output>
After completion, create `.planning/phases/10-calculator-per-person-breakdown/10-01-SUMMARY.md`
</output>
