---
phase: 12-google-maps-driving-route
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Map.astro
  - .env
  - .env.example
autonomous: false
requirements:
  - MAPS-01
  - MAPS-02
user_setup:
  - service: google-maps-embed-api
    why: "Embed API directions mode requires an API key from Google Cloud Console"
    env_vars:
      - name: PUBLIC_GOOGLE_MAPS_KEY
        source: "Google Cloud Console -> APIs & Services -> Credentials -> Create API Key"
    dashboard_config:
      - task: "Enable Maps Embed API"
        location: "Google Cloud Console -> APIs & Services -> Library -> Maps Embed API -> Enable"
      - task: "Restrict API key to HTTP referrers"
        location: "Google Cloud Console -> API key details -> Application restrictions -> HTTP referrers -> Add https://timberandthreadsretreat.com and https://timberandthreadsretreat.com/*"
      - task: "Restrict API key to Maps Embed API only"
        location: "Google Cloud Console -> API key details -> API restrictions -> Restrict key -> Select Maps Embed API"

must_haves:
  truths:
    - "The map section displays an interactive driving route (blue route line) from the Highway 7 / Breaktime area to 306 NW 300 Rd, Clinton, MO 64735"
    - "The route follows Highway 7 -> NW 221 Rd -> property (matching the text directions), not an arbitrary Google-chosen alternate route"
    - "A visitor can zoom and pan the embedded route map without leaving the page"
    - "An 'Open in Google Maps' link lets visitors launch turn-by-turn navigation from their current location"
    - "The Maps API key is stored as PUBLIC_GOOGLE_MAPS_KEY in .env (not committed to git) and Vercel dashboard"
    - "All 6 existing text direction steps, the GPS warning callout, and the address line remain unchanged"
  artifacts:
    - path: "src/components/Map.astro"
      provides: "Directions-mode Google Maps embed with driving route, 'Open in Google Maps' link"
      contains: "maps/embed/v1/directions"
    - path: ".env.example"
      provides: "Documentation of PUBLIC_GOOGLE_MAPS_KEY variable"
      contains: "PUBLIC_GOOGLE_MAPS_KEY"
    - path: ".env"
      provides: "Actual API key value for local development"
      contains: "PUBLIC_GOOGLE_MAPS_KEY"
  key_links:
    - from: "src/components/Map.astro"
      to: "import.meta.env.PUBLIC_GOOGLE_MAPS_KEY"
      via: "Astro frontmatter env var access"
      pattern: "import\\.meta\\.env\\.PUBLIC_GOOGLE_MAPS_KEY"
    - from: "Map.astro iframe data-src"
      to: "IntersectionObserver script"
      via: "dataset.src -> iframe.src swap on scroll"
      pattern: "dataset\\.src"
---

<objective>
Replace the static Google Maps location pin with an interactive driving route from the Highway 7 / Breaktime area to the retreat property, using the free Google Maps Embed API directions mode. Add an "Open in Google Maps" link for turn-by-turn navigation.

Purpose: First-time visitors can see the exact route they need to follow, matching the text directions. The visual route supplements the battle-tested written directions.
Output: Updated Map.astro with directions-mode iframe, "Open in Google Maps" link, and environment variable wiring for the API key.
</objective>

<execution_context>
@/home/evan/.claude/get-shit-done/workflows/execute-plan.md
@/home/evan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-google-maps-driving-route/12-CONTEXT.md
@.planning/phases/12-google-maps-driving-route/12-RESEARCH.md
@src/components/Map.astro
@.env.example

<interfaces>
<!-- Existing Map.astro iframe lazy-load pattern (MUST be preserved): -->
<!-- The <script> block reads dataset.src and assigns to iframe.src on IntersectionObserver trigger -->
<!-- This pattern is established across Calendar and Map embeds — do not change it -->

From src/components/Map.astro (existing script block — keep unchanged):
```typescript
const iframe = document.getElementById('maps-iframe') as HTMLIFrameElement | null;
if (iframe) {
  const observer = new IntersectionObserver(
    (entries) => {
      if (entries[0].isIntersecting) {
        const src = iframe.dataset.src;
        if (src) iframe.src = src;
        observer.disconnect();
      }
    },
    { rootMargin: '200px 0px' }
  );
  observer.observe(iframe);
}
```

From .env.example (existing env var pattern):
```bash
# Resend API key -- get from resend.com dashboard -> API Keys
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxx
# Email address where contact form submissions are delivered
OWNER_EMAIL=timberandthreads24@gmail.com
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create Google Maps Embed API key in Google Cloud Console</name>
  <action>
The executor cannot create Google Cloud API keys. The user must complete this setup.

**What the user needs to do:**

1. Go to https://console.cloud.google.com/
2. Create a new project or select an existing one for Timber & Threads
3. Enable billing on the project (required even though Embed API is free — zero charges will accrue)
4. Go to APIs & Services -> Library -> search "Maps Embed API" -> Enable it
5. Go to APIs & Services -> Credentials -> Create Credentials -> API Key
6. Click "Edit API Key" on the newly created key:
   - **Application restrictions:** Select "HTTP referrers (websites)"
   - **Website restrictions:** Add these two entries:
     - `https://timberandthreadsretreat.com`
     - `https://timberandthreadsretreat.com/*`
   - (Optional for local dev: temporarily add `http://localhost:*` — remove before go-live)
   - **API restrictions:** Select "Restrict key" -> check only "Maps Embed API"
7. Copy the API key value (starts with `AIza...`)
8. Paste it into the project `.env` file as: `PUBLIC_GOOGLE_MAPS_KEY=AIza...`
9. Add the same key to Vercel Dashboard -> Project Settings -> Environment Variables as `PUBLIC_GOOGLE_MAPS_KEY`

**Note:** The key is `PUBLIC_` prefixed because it is embedded in an iframe URL visible in HTML. Domain restriction + API restriction prevent misuse even if scraped. This follows Astro's convention for build-time-inlined client-visible values.
  </action>
  <resume-signal>Paste the API key value or confirm it has been added to .env. Type "done" when complete.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update Map.astro with directions route, "Open in Google Maps" link, and env vars</name>
  <files>
    src/components/Map.astro
    .env.example
  </files>
  <action>
**Map.astro changes:**

1. **Add frontmatter block** at the top of Map.astro (before the existing `---` block) to construct the Embed API directions URL:

```astro
---
import { Image } from 'astro:assets';
import entranceDriveway from '../assets/images/entrance-driveway-summer.jpg';

const mapsKey = import.meta.env.PUBLIC_GOOGLE_MAPS_KEY;

const origin = encodeURIComponent('215 NW Highway 7, Clinton, MO 64735');
const destination = encodeURIComponent('306 NW 300 Rd, Clinton, MO 64735');
const waypoint = encodeURIComponent('NW 221 Rd, Clinton, MO 64735');

const mapSrc = `https://www.google.com/maps/embed/v1/directions?key=${mapsKey}&origin=${origin}&destination=${destination}&waypoints=${waypoint}&mode=driving&maptype=roadmap`;
---
```

The `waypoint` parameter forces the route through NW 221 Rd, preventing Google from picking an alternate "faster" route that doesn't match the text directions.

2. **Replace the iframe `data-src`** value. Change from the legacy `pb=!1m18...` URL to the new `mapSrc` variable:

```html
<iframe
  id="maps-iframe"
  data-src={mapSrc}
  class="absolute top-0 left-0 w-full h-full border-0"
  frameborder="0"
  scrolling="no"
  title="Driving directions to Timber & Threads Retreat"
  aria-label="Google Maps showing driving route to retreat"
  allowfullscreen
  loading="lazy"
  referrerpolicy="no-referrer-when-downgrade"
></iframe>
```

Key changes from the existing iframe:
- `data-src` uses `{mapSrc}` variable (Astro expression) instead of hardcoded legacy URL
- `title` updated to "Driving directions to Timber & Threads Retreat" (was "Timber & Threads Retreat location")
- `aria-label` updated to "Google Maps showing driving route to retreat" (was "showing retreat location")
- All other attributes remain the same (id, class, frameborder, scrolling, allowfullscreen, loading, referrerpolicy)

3. **Add "Open in Google Maps" link** below the address line, inside the map column's card. Place it after the existing `<p>` with the address text:

```html
<p class="text-center mt-4 mb-2 text-stone-600">306 NW 300 Rd, Clinton, MO 64735</p>
<p class="text-center mb-4">
  <a
    href="https://www.google.com/maps/dir/?api=1&destination=306+NW+300+Rd%2CClinton%2CMO+64735&travelmode=driving"
    target="_blank"
    rel="noopener noreferrer"
    class="inline-flex items-center gap-1.5 text-brand hover:text-teal-700 font-medium transition-colors"
  >
    <svg class="w-4 h-4" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
      <path fill-rule="evenodd" d="M4.25 5.5a.75.75 0 00-.75.75v8.5c0 .414.336.75.75.75h8.5a.75.75 0 00.75-.75v-4a.75.75 0 011.5 0v4A2.25 2.25 0 0112.75 17h-8.5A2.25 2.25 0 012 14.75v-8.5A2.25 2.25 0 014.25 4h5a.75.75 0 010 1.5h-5zm7.25-.75a.75.75 0 01.75-.75h3.25a.75.75 0 01.75.75v3.25a.75.75 0 01-1.5 0v-1.44l-4.72 4.72a.75.75 0 01-1.06-1.06l4.72-4.72h-1.44a.75.75 0 01-.75-.75z" clip-rule="evenodd" />
    </svg>
    Open in Google Maps
  </a>
</p>
```

This link uses the Maps URLs API (no API key needed). Omitting `origin` causes Google Maps to use the visitor's current location for turn-by-turn navigation. The external-link SVG icon signals that this opens a new tab.

Note: Change the existing address `<p>` margin from `mb-4` to `mb-2` so there's proper spacing between address and the new link.

4. **Keep the existing `<script>` block unchanged.** The IntersectionObserver lazy-load pattern reads `dataset.src` and sets `iframe.src` on scroll. No changes needed — the new `data-src` value is still a URL string that the observer will swap in.

5. **Keep ALL existing content unchanged:**
   - All 6 driving direction steps
   - The GPS warning callout (amber box)
   - The entrance photo section
   - The section heading ("Find Us")
   - The two-column grid layout
   - Mobile stacking behavior
   - The 4:3 aspect ratio (75% padding-top)

**.env.example changes:**

Add the new variable below the existing entries:

```bash
# Google Maps Embed API key -- get from Google Cloud Console -> APIs & Services -> Credentials
# Must be restricted to timberandthreadsretreat.com domain and Maps Embed API only
PUBLIC_GOOGLE_MAPS_KEY=your_maps_embed_api_key_here
```
  </action>
  <verify>
    <automated>cd /home/evan/Projects/clients/timberandthreads-v2 && npx astro check 2>&1 | tail -20 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
Map.astro uses the Embed API directions URL with origin (Breaktime on Hwy 7), destination (306 NW 300 Rd), and waypoint (NW 221 Rd). "Open in Google Maps" link present below address. API key accessed via import.meta.env.PUBLIC_GOOGLE_MAPS_KEY. .env.example documents the new variable. Build succeeds with no errors. All 6 text direction steps, GPS warning, entrance photo, and two-column layout are unchanged.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify driving route matches text directions</name>
  <action>
The embedded map now shows a driving route from the Breaktime / Highway 7 area to 306 NW 300 Rd, Clinton, MO. The route should follow NW 221 Rd through to the property, matching the text directions.

**Verification steps:**
1. Run `npm run dev` and open http://localhost:4321 in a browser
2. Scroll down to the "Find Us" / Location section
3. Verify the map shows a **blue driving route line** (not just a location pin)
4. Verify the route follows: Highway 7 → NW 221 Rd → left turn → to the property gate area
5. Verify you can **zoom and pan** the map interactively
6. Verify the "Open in Google Maps" link appears below the address — click it and confirm it opens Google Maps with the destination pre-filled
7. Verify all 6 text driving directions are still present and unchanged in the right column
8. Verify the GPS warning callout (amber box) is still present
9. Verify the entrance photo is still displayed below the map/directions area
10. Check on a narrow viewport (375px) — map should stack above directions

**If the route does NOT match the text directions** (e.g., Google chose a different road):
- Report which road the route uses instead of NW 221 Rd
- The waypoint may need lat/lng coordinates instead of an address: `38.3870,-93.7330` for NW 221 Rd
  </action>
  <verify>Visual inspection by user confirms route matches text directions</verify>
  <done>Route displays correctly from Highway 7 / Breaktime to property via NW 221 Rd. Map is interactive (zoom/pan). "Open in Google Maps" link works. All existing content preserved.</done>
  <resume-signal>Type "approved" if the route matches the text directions, or describe what's wrong with the route</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` completes with zero errors
2. The built HTML contains a `maps/embed/v1/directions` URL (not the legacy `pb=` format)
3. The iframe `data-src` references `import.meta.env.PUBLIC_GOOGLE_MAPS_KEY` (inlined at build time)
4. `.env.example` documents `PUBLIC_GOOGLE_MAPS_KEY`
5. `.env` contains the actual API key (not committed to git — .gitignore covers .env)
6. The map displays an interactive driving route, not a static pin
7. The "Open in Google Maps" link opens turn-by-turn navigation to the destination
8. All existing text directions, GPS warning, entrance photo, and layout are preserved
</verification>

<success_criteria>
- The embedded map shows a visual driving route from Highway 7 / Breaktime to 306 NW 300 Rd, Clinton, MO 64735
- The route follows the same path as the text directions (Highway 7 → NW 221 Rd → property)
- Visitors can zoom and pan the route map
- "Open in Google Maps" link launches turn-by-turn navigation from the visitor's current location
- API key is in .env (not git) and Vercel dashboard, restricted to production domain and Maps Embed API
- All existing content (6 direction steps, GPS warning, address, entrance photo) is unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/12-google-maps-driving-route/12-01-SUMMARY.md`
</output>
