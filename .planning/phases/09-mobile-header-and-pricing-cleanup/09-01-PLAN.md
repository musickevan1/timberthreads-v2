---
phase: 09-mobile-header-and-pricing-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Nav.astro
  - src/components/Accommodations.astro
autonomous: false
requirements:
  - MOBL-01
  - PRIC-07

must_haves:
  truths:
    - "On a 375px mobile viewport, the header displays 'Timber & Threads' text alongside the logo without overflowing or pushing the hamburger button off-screen"
    - "On a 320px mobile viewport, the header text remains legible and the layout does not overflow horizontally"
    - "The Accommodations section contains no duplicate pricing tier cards — pricing cards appear only in the Pricing/Calculator section"
    - "A brief pricing teaser in the Accommodations section links to the pricing calculator section"
    - "The desktop layout at 1280px is unchanged — sticky nav, logo, brand text, and links all render correctly"
  artifacts:
    - path: "src/components/Nav.astro"
      provides: "Brand text visible on all viewports"
      contains: "text-xl sm:text-2xl"
    - path: "src/components/Accommodations.astro"
      provides: "Accommodations section without duplicate rate card, with pricing teaser"
      contains: "#pricing-calculator"
  key_links:
    - from: "src/components/Accommodations.astro"
      to: "#pricing-calculator"
      via: "anchor href in pricing teaser paragraph"
      pattern: 'href="#pricing-calculator"'
---

<objective>
Fix mobile header branding and remove duplicate pricing cards from Accommodations section.

Purpose: The mobile header currently hides the retreat name, leaving only a small logo — first-time mobile visitors have no text indicating what site they're on. The Accommodations section duplicates pricing tier cards that already exist in PricingSection.astro, creating content redundancy and potential maintenance drift.

Output: Nav.astro with brand text visible on all viewports; Accommodations.astro with rate card block removed and replaced by a one-line pricing teaser linking to the calculator.
</objective>

<execution_context>
@/home/evan/.claude/get-shit-done/workflows/execute-plan.md
@/home/evan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-mobile-header-and-pricing-cleanup/09-RESEARCH.md

<interfaces>
<!-- Key markup the executor needs. Extracted from source inspection. -->

From src/components/Nav.astro (lines 20-34, brand section):
```html
<!-- Brand -->
<a href="#home" class="flex items-center gap-2 shrink-0 group" aria-label="Timber & Threads Retreat — back to top">
  <Image src={logoImage} alt="" width={32} height={32} loading="eager" class="h-8 w-8 object-contain" />
  <div class="flex flex-col">
    <span class="font-serif text-2xl text-stone-800 group-hover:text-brand transition-colors hidden sm:block">Timber &amp; Threads</span>
    <div class="w-0 h-1 bg-brand group-hover:w-full transition-all duration-300 hidden sm:block"></div>
  </div>
</a>
```

From src/components/Accommodations.astro (lines 122-150, end of cards grid through rate card):
```html
    </div>  <!-- closes grid div at line 122 -->

    <!-- Rate Card -->
    <div class="mt-12 bg-white rounded-lg shadow-sm p-8 max-w-3xl mx-auto text-center">
      <h3 class="font-serif text-2xl text-stone-800 mb-6">Pricing</h3>
      <div class="grid sm:grid-cols-2 gap-6 mb-6">
        <!-- Standard tier -->
        <div class="bg-stone-50 rounded-lg p-6">
          ...standard tier content...
        </div>
        <!-- Large group tier -->
        <div class="bg-stone-50 rounded-lg p-6">
          ...large group content...
        </div>
      </div>
      <p class="text-stone-500 text-base mb-6">Minimum 4 persons &middot; Minimum 2-night stay</p>
      <a href="#pricing-calculator" class="inline-flex items-center px-6 py-3 bg-brand text-white rounded-lg font-medium hover:bg-brand-dark transition-colors">
        Estimate Your Stay
      </a>
    </div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Show brand text on mobile and remove duplicate pricing cards</name>
  <files>src/components/Nav.astro, src/components/Accommodations.astro</files>
  <action>
**Nav.astro — MOBL-01:**

On line 31, the brand name `<span>` has classes `font-serif text-2xl text-stone-800 group-hover:text-brand transition-colors hidden sm:block`.

Make these two changes to that span's class attribute:
1. Remove `hidden sm:block` (this is what hides the brand text on mobile)
2. Change `text-2xl` to `text-xl sm:text-2xl` (smaller font on mobile to fit at narrow viewports)

The resulting span class should be: `font-serif text-xl sm:text-2xl text-stone-800 group-hover:text-brand transition-colors`

IMPORTANT: Do NOT touch line 32's `<div>` element — it must keep its `hidden sm:block` classes. The animated underline decoration is a hover effect and should remain hidden on mobile.

**Accommodations.astro — PRIC-07:**

Delete the entire Rate Card block from lines 124 through 150 inclusive. This starts with the `<!-- Rate Card -->` comment and ends with the closing `</div>` of the `mt-12` wrapper.

In its place, insert this pricing teaser paragraph (after the closing `</div>` of the cards grid on line 122):

```html
    <p class="text-center text-stone-600 mt-8">
      Stays from <strong class="text-stone-800">$60/night per person</strong> — <a href="#pricing-calculator" class="text-brand hover:underline">see full pricing and estimate your stay</a>.
    </p>
```

This teaser mentions only the $60/night base rate per the locked decision. It does NOT include the large group flat rate or meal option. The link uses a plain anchor to `#pricing-calculator` which benefits from the existing `scroll-behavior: smooth` on the `<html>` element.
  </action>
  <verify>
    <automated>cd /home/evan/Projects/clients/timberandthreads-v2 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
- Nav.astro brand span no longer has `hidden sm:block` and uses `text-xl sm:text-2xl`
- Nav.astro underline div still has `hidden sm:block`
- Accommodations.astro has no Rate Card block (no `mt-12` pricing div, no tier cards, no "Estimate Your Stay" button)
- Accommodations.astro has a one-line pricing teaser with `$60/night per person` and a link to `#pricing-calculator`
- `npm run build` succeeds with zero errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify mobile header and accommodations section at all viewports</name>
  <files>src/components/Nav.astro, src/components/Accommodations.astro</files>
  <action>
Human verification checkpoint. Run `npm run dev` and visually inspect the changes at multiple viewport widths.

**What was built:**
Mobile header brand text now visible on all viewports. Duplicate pricing cards removed from Accommodations section and replaced with a one-line pricing teaser linking to the calculator.

**How to verify:**

Run `npm run dev` and open http://localhost:4321 in your browser.

**Mobile header (375px):**
1. Open DevTools, toggle device toolbar, set viewport to 375px wide (iPhone SE)
2. Verify "Timber & Threads" text is visible next to the logo in the header
3. Verify the hamburger button is still visible and not pushed off-screen
4. Verify no horizontal scrollbar appears

**Mobile header (320px):**
5. Set viewport to 320px wide
6. Verify "Timber & Threads" text is still legible and not cut off
7. Verify no horizontal overflow

**Desktop (1280px):**
8. Set viewport to 1280px wide
9. Verify the sticky nav looks identical to before — logo, "Timber & Threads" text, navigation links, all rendering correctly
10. Verify the animated underline decoration still appears on hover over the brand name

**Accommodations section:**
11. Scroll to the Accommodations section
12. Verify the two feature cards (Comfortable Accommodations, Creative Workspaces) are present
13. Verify there are NO pricing tier cards below the feature cards
14. Verify there is a one-line pricing teaser that says "Stays from $60/night per person" with a link
15. Click the pricing teaser link — it should smooth-scroll to the Pricing Calculator section
  </action>
  <verify>
    <automated>cd /home/evan/Projects/clients/timberandthreads-v2 && grep -c "hidden sm:block" src/components/Nav.astro && grep -c "Rate Card" src/components/Accommodations.astro</automated>
  </verify>
  <done>User confirms brand text visible at 375px and 320px, no overflow, desktop unchanged, no duplicate pricing cards, teaser link works</done>
</task>

</tasks>

<verification>
- `npm run build` completes with zero errors
- `grep -c "hidden sm:block" src/components/Nav.astro` returns 1 (only the underline div, not the brand span)
- `grep -c "Rate Card" src/components/Accommodations.astro` returns 0
- `grep -c "pricing-calculator" src/components/Accommodations.astro` returns 1 (the teaser link)
- Visual inspection at 320px, 375px, and 1280px confirms no layout breakage
</verification>

<success_criteria>
1. On mobile viewports (375px and 320px), "Timber & Threads" text is visible in the header alongside the logo
2. No horizontal overflow at 320px minimum viewport
3. Desktop layout at 1280px is visually unchanged
4. Accommodations section has zero pricing tier cards — only the two feature cards and a pricing teaser line
5. Pricing teaser links to `#pricing-calculator` and smooth-scrolls correctly
</success_criteria>

<output>
After completion, create `.planning/phases/09-mobile-header-and-pricing-cleanup/09-01-SUMMARY.md`
</output>
