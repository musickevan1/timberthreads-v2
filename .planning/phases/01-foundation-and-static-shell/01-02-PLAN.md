---
phase: 01-foundation-and-static-shell
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/layouts/BaseLayout.astro
  - src/components/Nav.astro
  - src/components/Hero.astro
  - src/components/Footer.astro
  - src/scripts/scroll-spy.js
  - src/pages/index.astro
  - src/assets/images/hero-front-view.jpeg
  - src/assets/images/logo.png
autonomous: true
requirements:
  - FOUND-02
  - FOUND-03
  - FOUND-04
must_haves:
  truths:
    - "The site displays a sticky navigation bar with links for Home, About, Retreats, Accommodations, Calendar, Gallery, Contact, Location"
    - "Clicking a nav link smooth-scrolls to the corresponding placeholder section anchor"
    - "Scrolling through sections updates the active nav link indicator via scroll-spy"
    - "The hero section fills the full viewport height with a background image, logo, tagline, and two CTA buttons"
    - "On mobile (<768px), the desktop nav links are hidden and a hamburger menu opens a full-screen overlay with large tap targets"
    - "The layout is responsive: content reflows correctly at mobile, tablet (md), and desktop (lg) breakpoints"
    - "The page has proper meta tags, Open Graph tags, and the Inter font loaded"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "HTML shell with head (meta, OG, fonts), Nav, slot, Footer"
      contains: "og:title"
    - path: "src/components/Nav.astro"
      provides: "Sticky nav with desktop links, mobile hamburger, scroll-spy integration"
      contains: "aria-current"
    - path: "src/components/Hero.astro"
      provides: "Full-viewport hero with two-layer structure (background image + foreground content)"
      contains: "h-screen"
    - path: "src/components/Footer.astro"
      provides: "Footer with copyright, contact info, condensed nav"
      contains: "footer"
    - path: "src/scripts/scroll-spy.js"
      provides: "IntersectionObserver-based active section tracking"
      contains: "IntersectionObserver"
    - path: "src/pages/index.astro"
      provides: "Single-page layout with all section placeholders"
      contains: "BaseLayout"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/Nav.astro"
      via: "component import"
      pattern: "import Nav from"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/styles/global.css"
      via: "CSS import"
      pattern: "import.*global\\.css"
    - from: "src/components/Nav.astro"
      to: "src/scripts/scroll-spy.js"
      via: "script import"
      pattern: "import.*scroll-spy"
    - from: "src/pages/index.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "layout wrapper"
      pattern: "BaseLayout"
    - from: "src/pages/index.astro"
      to: "src/components/Hero.astro"
      via: "component import"
      pattern: "import Hero from"
---

<objective>
Build the BaseLayout, navigation with scroll-spy, hero section, footer, and single-page index with placeholder sections -- creating the responsive shell that all content phases will fill in.

Purpose: The layout shell is the backbone of the single-page site. Every content section in Phase 2+ drops into the placeholder anchors created here. Navigation, responsive behavior, and visual identity must be correct before adding content.
Output: A fully navigable single-page shell with sticky nav, scroll-spy, full-viewport hero with the retreat's front photo, responsive mobile menu, footer, and placeholder sections for all content areas.
</objective>

<execution_context>
@/home/evan/.claude/get-shit-done/workflows/execute-plan.md
@/home/evan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-static-shell/01-RESEARCH.md
@.planning/phases/01-foundation-and-static-shell/01-CONTEXT.md
@.planning/phases/01-foundation-and-static-shell/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BaseLayout, Hero, Footer, and index page with section placeholders</name>
  <files>
    src/layouts/BaseLayout.astro
    src/components/Hero.astro
    src/components/Footer.astro
    src/pages/index.astro
    src/assets/images/hero-front-view.jpeg
    src/assets/images/logo.png
  </files>
  <action>
    **First, copy images from the old site for Phase 1 use:**
    ```bash
    mkdir -p src/assets/images
    cp ../timberandthreads/public/assets/gallery/hero-front-view.jpeg src/assets/images/
    cp ../timberandthreads/public/assets/gallery/logo.png src/assets/images/
    ```

    **Create `src/layouts/BaseLayout.astro`:**

    This is the HTML shell for every page. It must:
    - Import `@fontsource-variable/inter` for the self-hosted Inter font
    - Import `../styles/global.css` for Tailwind + theme
    - Accept props: `title` (default: "Timber & Threads Retreat"), `description` (default: "A quilting and crafting retreat on a private island in the Missouri Ozarks"), `ogImage` (default: "/og-default.jpg")
    - Include complete `<head>` with: charset, viewport, title, meta description, Open Graph tags (og:title, og:description, og:image, og:type="website", og:url), favicon link, named slot `<slot name="head" />` for page-specific head content
    - Body structure: `<Nav />` component, `<main><slot /></main>`, `<Footer />` component

    **Create `src/components/Hero.astro`:**

    Two-layer hero structure (background + foreground) designed for future video swap in Phase 3.
    - Outer: `<section id="home" class="relative h-screen overflow-hidden">`
    - Background layer: `<div class="absolute inset-0 z-0">` containing:
      - Astro `<Image>` component with the hero-front-view.jpeg from `src/assets/images/`, `loading="eager"`, `fetchpriority="high"`, `widths={[768, 1280, 1920]}`, `sizes="100vw"`, `class="w-full h-full object-cover object-center"`, descriptive alt text
      - Dark overlay: `<div class="absolute inset-0 bg-black/30"></div>`
    - Foreground layer: `<div class="relative z-10 flex flex-col items-center justify-center h-full text-center text-white px-4">` containing:
      - Logo image (use `<Image>` for the logo.png, reasonable width like 120-140px, `loading="eager"`)
      - `<h1>` with "Timber & Threads Retreat" in `font-serif text-4xl md:text-5xl lg:text-6xl`
      - `<p>` tagline "Relax, create, and connect in nature's embrace" in `text-xl md:text-2xl`
      - Two CTA buttons in a flex row:
        - "Learn More" → `href="#about"` with `bg-brand hover:bg-brand-dark text-white px-6 py-3 rounded-lg transition-colors`
        - "Contact Us to Book" → `href="#contact"` with `border-2 border-white text-white hover:bg-white hover:text-stone-900 px-6 py-3 rounded-lg transition-colors`

    **Create `src/components/Footer.astro`:**

    Minimal footer (Claude's discretion: three-column desktop, single-column mobile). Must NOT include Admin link (locked decision).
    - Background: `bg-stone-900 text-stone-300`
    - Three columns on md+ (grid or flex):
      - Column 1: "Timber & Threads Retreat" brand text + brief tagline
      - Column 2: Condensed nav links (same section order as main nav)
      - Column 3: Contact info -- phone (417) 343-1473 as `tel:` link, email timberandthreads24@gmail.com as `mailto:` link, address
    - Copyright line at bottom: "(c) {currentYear} Timber & Threads Retreat"
    - Use `new Date().getFullYear()` in Astro frontmatter for dynamic year

    **Create `src/pages/index.astro`:**

    The single-page layout that imports BaseLayout, Hero, and creates placeholder sections for every content area. Each placeholder section needs:
    - A `<section id="sectionId">` with the correct anchor ID matching nav links
    - Alternating backgrounds per locked decision: white, stone-50, teal-50/10, stone-50, white, etc.
    - A centered heading with the section name (e.g., "About", "Retreats")
    - A brief placeholder paragraph like "Content coming in Phase 2"
    - Minimum height of `min-h-[50vh]` so scroll-spy has enough content to trigger

    Section IDs and order (matching nav): `home` (Hero), `about`, `retreats`, `accommodations`, `calendar`, `gallery`, `contact`, `location`

    Use consistent section padding: `py-16 md:py-24` with content in `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8`
  </action>
  <verify>
    Run `npm run build` to confirm zero errors. Run `npm run dev` and visually confirm in the browser:
    1. Hero fills the viewport with the retreat front photo, logo, tagline, and two CTA buttons
    2. Scrolling reveals placeholder sections with alternating backgrounds
    3. Footer appears at the bottom with three columns on desktop, single column on mobile
    4. Page meta tags and OG tags are present in the HTML source
  </verify>
  <done>
    BaseLayout renders the full page shell with meta/OG tags, Inter font loaded, Nav and Footer components. Hero fills the viewport with the two-layer background+foreground structure, displaying the retreat photo, logo, tagline, and CTA buttons. Index page contains 8 placeholder sections with alternating backgrounds and correct anchor IDs matching the nav links.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create sticky navigation with scroll-spy and mobile menu</name>
  <files>
    src/components/Nav.astro
    src/scripts/scroll-spy.js
  </files>
  <action>
    **Create `src/scripts/scroll-spy.js`:**

    Vanilla JS IntersectionObserver that tracks which section is currently in view and sets `aria-current="true"` on the matching nav link.

    ```javascript
    const sections = document.querySelectorAll('section[id]');
    const navLinks = document.querySelectorAll('nav a[href^="#"]');

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            navLinks.forEach((link) => {
              const isActive = link.getAttribute('href') === `#${entry.target.id}`;
              link.setAttribute('aria-current', isActive ? 'true' : 'false');
            });
          }
        });
      },
      {
        // Thin detection band near top of viewport.
        // Section is "active" when its top edge crosses the top ~10%.
        rootMargin: '-5% 0% -90% 0%',
      }
    );

    sections.forEach((section) => observer.observe(section));
    ```

    **Create `src/components/Nav.astro`:**

    Sticky navigation with desktop links, mobile hamburger, and scroll-spy integration.

    Structure:
    - `<header class="sticky top-0 z-50 bg-white/95 backdrop-blur-sm shadow-sm">` (Claude's discretion: frosted glass effect)
    - Inner container: `<nav class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 flex items-center justify-between h-16">`
    - Left: Brand link (`<a href="#home">`) with logo image (small, ~32px height) + "Timber & Threads" text in `font-serif text-lg text-stone-900`
    - Right desktop: `<ul class="hidden md:flex gap-1 lg:gap-4">` with nav items
    - Right mobile: hamburger button `<button>` visible only on `md:hidden`

    Nav items array (defined in frontmatter):
    ```javascript
    const navItems = [
      { label: 'Home', href: '#home' },
      { label: 'About', href: '#about' },
      { label: 'Retreats', href: '#retreats' },
      { label: 'Accommodations', href: '#accommodations' },
      { label: 'Calendar', href: '#calendar' },
      { label: 'Gallery', href: '#gallery' },
      { label: 'Contact', href: '#contact' },
      { label: 'Location', href: '#location' },
    ];
    ```

    Desktop nav link styling:
    - Base: `text-stone-600 hover:text-brand transition-colors text-sm px-2 py-1`
    - Active state via `aria-current`: Use Tailwind arbitrary attribute selector `[&[aria-current='true']]:text-brand [&[aria-current='true']]:border-b-2 [&[aria-current='true']]:border-brand`

    Mobile menu (Claude's discretion: full-screen overlay with large tap targets for older demographic):
    - `<div id="mobile-menu" class="hidden fixed inset-0 bg-white z-40">` with close button at top-right
    - Nav links centered vertically in a flex column with `gap-6`
    - Each link: `text-2xl font-serif text-stone-900 hover:text-brand py-3` (minimum 56px tap target height)
    - Clicking any mobile nav link closes the menu and scrolls to section

    Hamburger button:
    - `aria-label="Open navigation menu"`, `aria-expanded="false"`
    - Simple three-line SVG icon (24x24), transforms to X when menu open (toggle via class)

    Script section (inside `<script>` tag in Nav.astro):
    - Import scroll-spy: `import '../scripts/scroll-spy.js';`
    - Mobile menu toggle: listen for click on hamburger button, toggle `hidden` class on mobile menu, toggle `aria-expanded`
    - Close on link click: all mobile menu `<a>` tags close the menu on click
    - Close on Escape key: `document.addEventListener('keydown', ...)` closes menu on Escape
    - Body scroll lock: when mobile menu is open, add `overflow-hidden` to `document.body`; remove when closed
  </action>
  <verify>
    Run `npm run build` to confirm zero errors. Run `npm run dev` and test in browser:
    1. Nav sticks to top on scroll with frosted glass effect
    2. Clicking "About" smoothly scrolls to the #about placeholder section
    3. Scrolling through sections highlights the corresponding nav link with teal color + bottom border
    4. On mobile viewport (<768px): desktop links hidden, hamburger visible
    5. Clicking hamburger opens full-screen overlay with large nav links
    6. Clicking a mobile nav link scrolls to section AND closes the overlay
    7. Pressing Escape closes the mobile menu
    8. Body doesn't scroll when mobile menu is open
  </verify>
  <done>
    Sticky navigation with 8 section links is fully functional. Desktop shows inline links with scroll-spy active states (teal color + underline on current section). Mobile shows hamburger that opens a full-screen overlay with large tap targets. Smooth-scroll works for all anchor links. Scroll-spy correctly tracks the active section using IntersectionObserver. Mobile menu closes on link click, Escape key, and locks body scroll when open.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes with zero errors
2. BaseLayout includes meta description, og:title, og:description, og:image, og:type, og:url in `<head>`
3. Inter font is loaded (check network tab or rendered font-family)
4. Navigation is sticky and visible at all scroll positions
5. All 8 nav links scroll to their corresponding section anchors
6. Scroll-spy highlights the correct nav link as sections enter the viewport
7. Hero section fills 100vh with background image, dark overlay, logo, heading, tagline, two CTA buttons
8. CTA "Learn More" scrolls to #about, "Contact Us to Book" scrolls to #contact
9. Mobile menu (< md breakpoint): hamburger opens full-screen overlay, links close it, Escape closes it
10. Footer has brand, nav links, contact info, copyright with current year, NO admin link
11. Alternating section backgrounds visible (white, stone-50, teal-50, etc.)
12. Content is responsive: reflows at mobile (< sm), tablet (md), desktop (lg) breakpoints
</verification>

<success_criteria>
A visitor can load the site, see the full-screen hero with the retreat photo and CTA buttons, use the sticky navigation to smooth-scroll between 8 section placeholders, observe active-section highlighting as they scroll, open/close the mobile hamburger menu on small screens, and see a footer with contact info. The layout is responsive across all breakpoints.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-static-shell/01-02-SUMMARY.md`
</output>
